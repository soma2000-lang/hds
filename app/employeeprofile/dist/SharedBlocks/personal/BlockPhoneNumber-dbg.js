sap.ui.define(['sap/uxap/BlockBase'],
	function (BlockBase) {
		"use strict";

		var BlockPhoneNumber = BlockBase.extend("employeeprofile.SharedBlocks.personal.BlockPhoneNumber", {
			metadata: {}
		});


		return BlockPhoneNumber;

	});